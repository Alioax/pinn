# Archive

Superseded code kept for history and diffing against earlier reports. Do not use for new work.

- `legacy_review_ready/` — original minimal scripts (Pe v1 naming)
- `legacy_mainline_wip/` — earlier mainline PINN, parametric PINN, supervised/unsupervised DeepONet baselines
