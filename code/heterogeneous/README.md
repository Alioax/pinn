# Heterogeneous medium — neural operator (Report 5, in progress)

COMSOL reference data for multi-zone velocity fields lives in `data/`.

Planned: physics-informed neural operator over spatially varying advection (four-zone setup in `data/comsol_4zones.txt`).
