# jobs.txt - the scripts run.bat will execute, top to bottom.
#
#   * one script per line, paths relative to the repo root
#   * use FORWARD slashes (they work on Windows too)
#   * blank lines and lines starting with # are ignored
#   * add arguments after the path if your script takes them
#   * quote any path that contains spaces
#
# Workflow: edit this list on your laptop, commit & push, then run
# run.bat on the remote. The run stops at the first script that fails.
#
# ----- Report 5 batch E-F: dense architecture -----
# E: checkpoint already trained; validation-only to write CSV + run_meta.
code/heterogeneous/pino_heterogeneous/pinn1d_heterogeneous_parametric_neural_operator.py --design capacity --arch dense --out-dir results/exp_E_capacity_dense --validate-only --skip-validation-plots

# F: full train + validation.
code/heterogeneous/pino_heterogeneous/pinn1d_heterogeneous_parametric_neural_operator.py --design anchored --n-train 500 --n-corner-anchors 100 --arch dense --out-dir results/exp_F_anchored_N500_dense --skip-validation-plots
