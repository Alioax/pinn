# jobs.txt - the scripts run.bat will execute, top to bottom.
#
#   * one script per line, paths relative to the repo root
#   * use FORWARD slashes (they work on Windows too)
#   * blank lines and lines starting with # are ignored
#   * add arguments after the path if your script takes them
#   * quote any path that contains spaces
#
# Workflow: edit this list on your laptop, commit & push, then run
# run.bat on the remote. The run stops at the first script that fails.
#
# PINO_2 piecewise-only (exp_P): same arch/training as exp_O but 500 piecewise
#   cases (no GRF), zones 1-5 via maximin LHC, min zone width 10% of domain.
#
code/heterogeneous/pino_heterogeneous/pinn1d_heterogeneous_parametric_neural_operator.py --design grf --n-sensors 100 --n-grf 0 --n-piecewise-per-zones 100 --piecewise-zone-counts 1,2,3,4,5 --min-zone-frac 0.1 --arch w64 --dtype float64 --early-stop-patience 150 --lr-lbfgs 0.1 --lbfgs-max-iter 5 --epochs 1000 --out-dir results/exp_P_piecewise_K100_N500_z1to5_min10pct_w64_float64_lr01_maxiter5 --skip-validation-plots
