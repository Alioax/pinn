# jobs.txt - the scripts run.bat will execute, top to bottom.
#
#   * one script per line, paths relative to the repo root
#   * use FORWARD slashes (they work on Windows too)
#   * blank lines and lines starting with # are ignored
#   * add arguments after the path if your script takes them
#   * quote any path that contains spaces
#
# Workflow: edit this list on your laptop, commit & push, then run
# run.bat on the remote. The run stops at the first script that fails.
#
# Report 5 experiment F: maximin N=500 (same as C) with float64.
code/heterogeneous/pino_heterogeneous/pinn1d_heterogeneous_parametric_neural_operator.py --design maximin --n-train 500 --dtype float64 --early-stop-patience 150 --out-dir results/exp_F_maximin_N500_float64 --skip-validation-plots
#
# Report 5 experiment G: maximin N=500 (same as C) with one trunk per zone.
code/heterogeneous/pino_heterogeneous/pinn1d_heterogeneous_parametric_neural_operator.py --design maximin --n-train 500 --trunk-mode zone --early-stop-patience 150 --out-dir results/exp_G_maximin_N500_zone_trunks --skip-validation-plots
