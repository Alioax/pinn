# jobs.txt - the scripts run.bat will execute, top to bottom.
#
#   * one script per line, paths relative to the repo root
#   * use FORWARD slashes (they work on Windows too)
#   * blank lines and lines starting with # are ignored
#   * add arguments after the path if your script takes them
#   * quote any path that contains spaces
#
# Workflow: edit this list on your laptop, commit & push, then run
# run.bat on the remote. The run stops at the first script that fails.
#
# exp_Rb: same as Ra (lr=2.5e-3) but Adam 500 + L-BFGS 500 (maxiter 5).
#
code/heterogeneous/pino_heterogeneous/pinn1d_heterogeneous_parametric_neural_operator.py --design grf --n-sensors 100 --n-grf 0 --n-piecewise-per-zones 100 --piecewise-zone-counts 1,2,3,4,5 --min-zone-frac 0.1 --arch w64 --dtype float64 --early-stop-patience 150 --adam-epochs 500 --lr-adam 0.0025 --lr-lbfgs 0.1 --lbfgs-max-iter 5 --epochs 500 --out-dir results/exp_Rb_piecewise_K100_N500_adam500_lr25e3_lbfgs500_maxiter5_float64
